/**
 * Hardhat config (minimal, ESM)
 */

export default {
  solidity: "0.8.20",
  networks: {
    hardhat: {}
  },
  paths: {
    sources: "contracts",
    tests: "test",
    cache: "cache",
    artifacts: "artifacts"
  },
  mocha: {
    timeout: 200000
  }
};
